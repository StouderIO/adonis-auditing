{{{
  exports({ to: app.makePath(app.rcFile.directories.audit_resolvers, 'user_resolver.ts') })
}}}

import { HttpContext } from '@adonisjs/core/http'
import { UserResolver } from '@stouder-io/adonis-auditing'

export default class implements UserResolver {
  resolve({
    auth,
  }: HttpContext): Promise<InstanceType<ReturnType<ReturnType<typeof withAuthFinder>>>> {
    return auth.user ?? null
  }
}
